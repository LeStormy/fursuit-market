br
.container
  .card.horizontal.no-shadow
    .card-content
      .flow-text
        | INDEX
  .row
    - @builders.each do |builder|
      = render partial: "card", locals: {:builder => builder}

  .center-align
    = paginate @builders
